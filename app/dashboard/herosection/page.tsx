export const dynamic = "force-dynamic";
import React from "react";
import { HeroSectionDashboard } from "./HeroSection";
const HeroPage = () => {
  return <HeroSectionDashboard />;
};

export default HeroPage;
